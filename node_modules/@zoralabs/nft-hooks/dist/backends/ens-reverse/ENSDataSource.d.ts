import DataLoader from 'dataloader';
import { NetworkIDs } from '../../constants/networks';
import { ENSInterface, ENSInfo, ENSResultType } from './ENSInterface';
export declare class ENSDataSource implements ENSInterface {
    ensLoader: DataLoader<string, ENSInfo>;
    networkId: string;
    endpoint: string;
    timeout: number;
    constructor(networkId: NetworkIDs, timeout?: number, rpcUrl?: string | undefined);
    fetchEnsNames: (addresses: readonly string[]) => Promise<(Error | {
        name: any;
        address: string;
    })[]>;
    loadEnsFromAddresses: (addresses: string[]) => Promise<ENSResultType>;
}
