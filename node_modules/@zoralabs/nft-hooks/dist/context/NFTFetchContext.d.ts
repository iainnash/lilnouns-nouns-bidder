import React from 'react';
import { MediaFetchAgent } from '../fetcher/MediaFetchAgent';
import { NFTStrategy } from '../strategies/NFTStrategy';
import { NetworkIDs } from '../constants/networks';
import { ZoraV2IndexerStrategy } from '../strategies';
export declare type FetchContext = {
    strategy: typeof NFTStrategy;
};
export declare const defaultFetchAgent: {
    strategy: any;
    fetcher: any;
};
export declare const NFTFetchContext: React.Context<{
    strategy: NFTStrategy | ZoraV2IndexerStrategy;
    fetcher: MediaFetchAgent;
}>;
declare type NFTFetchConfigurationProps = {
    strategy?: NFTStrategy;
    networkId: NetworkIDs;
    children: React.ReactNode;
};
export declare const NFTFetchConfiguration: ({ strategy: userStrategy, children, networkId, }: NFTFetchConfigurationProps) => JSX.Element;
export {};
